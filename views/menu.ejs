<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Full Menu</title>
</head>

<body>
    <!-- Reuse the nav on this page -->
    <%- include("partials/nav") %>

        <h1>Full Menu</h1>

        <!--
      "menu" is an array sent from server.js like this:
      res.render("menu", { menu: RESTAURANT.menu })
    -->
        <ul>
            <% menu.forEach((item)=> { %>
                <li>
                    <!-- Show the item name -->
                    <h2>
                        <%= item.name %>

                            <!-- If rating is exactly 5, show a star -->
                            <% if (item.rating===5) { %>
                                ⭐
                                <% } %>
                    </h2>

                    <!-- Show key info -->
                    <p><strong>Price:</strong> $<%= item.price %>
                    </p>
                    <p><strong>Rating:</strong>
                        <%= item.rating %>
                    </p>
                    <p><strong>Category:</strong>
                        <%= item.category %>
                    </p>

                    <!-- Show details/description -->
                    <p>
                        <%= item.details %>
                    </p>
                </li>
                <hr />
                <% }) %>
        </ul>
</body>

</html>